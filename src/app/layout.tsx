import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Digital CA - Complete Accounting & GST Management',
  description: 'Comprehensive SaaS platform for accounting, GST, TDS, payroll, and compliance management',
  keywords: 'GST, accounting, TDS, payroll, compliance, CA, tax filing',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
